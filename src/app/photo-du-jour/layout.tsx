import { pageMetadata } from '@/lib/config/site'

export const metadata = pageMetadata('/photo-du-jour')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
