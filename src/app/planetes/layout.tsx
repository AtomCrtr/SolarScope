import { createPageMetadata } from '@/lib/site'

export const metadata = createPageMetadata('Planètes et lunes', 'Comparez les planètes du Système solaire et explorez-les en 3D.', '/planetes')
export default function Layout({ children }: { children: React.ReactNode }) { return children }
